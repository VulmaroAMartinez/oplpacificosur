/**
 * @deprecated Usar variables de entorno VITE_SUPABASE_URL y VITE_SUPABASE_ANON_KEY
 * con el cliente en src/supabaseClient.ts. Este archivo se mantiene solo por compatibilidad
 * con código legacy y no debe usarse en código nuevo.
 */
export const projectId = '';
export const publicAnonKey = '';
